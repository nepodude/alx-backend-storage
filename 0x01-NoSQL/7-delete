// Script to delete all documents in the 'school' collection with name="ALX School"
// Usage: cat 7-delete | mongo <database_name>

// Delete all matching documents and return the deletion result
const result = db.school.deleteMany({ name: "ALX School" });
printjson(result);
